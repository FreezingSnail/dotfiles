# Launch: alacritty --config-file ~/.config/alacritty/alacritty-nord.toml
# Reuses current font and glass settings; restores only the Nord palette.
import = [
  "~/.config/alacritty/alacritty.toml",
  "~/.config/alacritty/themes/themes/nord.toml",
]
